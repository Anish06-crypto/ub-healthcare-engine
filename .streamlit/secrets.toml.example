# ─────────────────────────────────────────────
# Streamlit Secrets — copy to .streamlit/secrets.toml for local use
# On Streamlit Community Cloud: paste the key-value pairs into
# App Settings → Secrets (without the .toml wrapper).
# ─────────────────────────────────────────────

# URL of the deployed FastAPI backend.
# Leave blank or omit to run in Demo Mode (mock data).
api_base = "https://your-backend.onrender.com"

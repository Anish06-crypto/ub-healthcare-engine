# ─────────────────────────────────────────────
# Dashboard dependencies — installed by Streamlit Cloud
# dashboard.py does NOT import pydantic / fastapi / groq / sqlalchemy.
# Backend packages live in requirements-backend.txt.
# ─────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.0.0
requests>=2.31.0

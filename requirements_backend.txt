# ─────────────────────────────────────────────
# FastAPI backend dependencies
# Install locally with:  pip install -r requirements_backend.txt
# Not used by Streamlit Cloud (dashboard only needs requirements.txt)
# ─────────────────────────────────────────────
fastapi==0.109.2
uvicorn==0.27.0
pydantic==2.6.1
groq==0.9.0
pytest==8.0.0
pytest-asyncio==0.23.5
httpx==0.26.0
python-dotenv==1.0.1
sqlalchemy>=2.0.0
requests>=2.31.0
slowapi==0.1.9
